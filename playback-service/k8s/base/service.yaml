apiVersion: v1
kind: Service
metadata:
  name: playback-service
spec:
  selector:
    app: playback-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080 # TODO: Change port if needed
